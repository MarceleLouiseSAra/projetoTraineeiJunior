import prisma from "../../database/prismaClient";
import { Request, Response, NextFunction } from "express";

export async function functionsName(req: Request, res: Response, next: NextFunction) {
    try {

    } catch (error) {

    }
}